import React from 'react';
import ReactDOM from 'react-dom';

import App from './App';

import './index.css';


ReactDOM.render(<App />, document.getElementById('root'));
// ^we are saying that this App component is going to be rendered inside a div w an ID of root //
